# Credenciais PÚBLICAS do Supabase (seguras no cliente — protegidas por RLS).
# Use a URL do projeto e a chave ANON / PUBLISHABLE (NUNCA a service/secret key:
# essa é de servidor e jamais entra no app).
#
# Copie para `.env` e preencha:
EXPO_PUBLIC_SUPABASE_URL=https://nqebfmyzchpkufsytvyf.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=cole_aqui_a_chave_anon_publishable
